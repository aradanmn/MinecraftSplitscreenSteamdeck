# Session 2026-06-26 — #37 Deck validation + away-work batch

## #37 validated on the Deck (deploy 6adab75)
- Quit in-game → watchdog detected `window … gone (player quit)` → SLOT_DIED → teardown
  force-killed the JVM at the exact `IAS unloaded` Bug-B point: **no 5-min hang.** Assumption
  (window leaves the tree at quit) holds. D6 still Abort-Game (separate, #15).
- Disconnect (DS4 power-off) → `instance PRESERVED (no teardown)`, slot stayed alive. ✅
- Found live: reconnect spawns a NEW instance that's **uncontrollable** — the orphaned slot
  still holds the reused node, so the new instance masked its OWN controller (N5 own-node bug).
  Logged to #20 + #37.

## Away-work batch (maintainer out 7a–5p; "keep busy")
- **N5 (#20) fixed** (1031ca5): `_build_bwrap_command` no longer masks a slot's own
  event/js node, and masks (not drops) a trailing unpaired mask arg + warns. Tests T4.10–12
  added (pass standalone). Removed dead T4.1 (`_write_splitscreen_properties`, feature gone).
  NOTE: test_instance_lifecycle.sh T4.7–T4.9 are pre-existing-stale (mock xdotool; detection
  moved to dex) and hang — separate repair.
- Watchdog cosmetic: dropped the duplicate "gone" in the window-gone SLOT_DIED log.

Pending (need maintainer/Deck): reconnect-adopt design (#37), N6/N7 security (entry script),
N10/D6 (on-Deck). test_orchestrator.sh + test_instance_lifecycle.sh T4.7–9 stale (separate).

## Away-work batch — addendum
- **N12/S4 fixed**: sanitize mod download filename to [A-Za-z0-9._-] (path-traversal hardening). Provably identical for valid names; verified.
- **N13 left for review**: `cp -r .../* ` empty-mod-set handling (instance_creation.sh ~485) — touches copy logic, not blind-safe.

---

## Controller isolation breakthrough + install validation (evening, 2026-06-26)

**D1 install validated end-to-end** on the Deck: fresh from-branch install (REPO_REF, commit-pinned) → 19 modules, preflight passed, Temurin JDK25, 4 instances MC 26.2 w/ Controlify+YACL+Fabric API (no Splitscreen mod), all 9 runtime modules deployed (verified = latest code), aradanmn accounts, honest success. Launch → P1 up, DS4 controls it. NOTE: README `/main/` installs the OLD FlyingEwok product (confirmed live) — README hardened; real install needs branch→main promotion. Added REPO_REF + preflight-sourcing + fail-loud + no-sudo install fixes (commits df020a2, cd42414, dd57715).

**Controller per-instance isolation — root-caused + fixed.** Ramping to 4 DS4s exposed: built-in leaked into P2, P1 erratic. Maintainer pushed back on every wrong theory of mine (pick-nondeterminism, N5). Proved live the bwrap `/dev/input` bind LEAKS: bound 1 pad, `/sys/class/input` listed all 9 controllers; `/run/udev` (547) + `~/.steam/steam.pipe` both reachable inside the sandbox. Deep-research + live testing converged: **SDL doesn't detect a plain bwrap as a container → stays on libudev enumeration**. Fix (in `_build_bwrap_command`): `--tmpfs /run/udev` + mask `steam.pipe` + **`SDL_JOYSTICK_DISABLE_UDEV=1`** (the key hint; `SDL_LINUX_JOYSTICK_CLASSIC` does nothing). Validated live, 4 DS4s: each DS4 → own player, built-in GONE, no cross-talk, input alive through the masked pipe. Residual cosmetic: 1/4 (the lone DS4 v1 054c:05c4) shows DS4 glyph not Xbox (#36) — `ALLOW_STEAM_VIRTUAL_GAMEPAD=0` is the untested next lever. Per-slot `/dev/null` masking now redundant (cleanup deferred, tested). Memory: controller-isolation-sdl-udev-leak.md.

---

## Raw-device controller bind — planned (4x) + implemented, gated default-OFF (late eve)

After proving the Steam-virtual-pad enumeration is fundamentally unreliable (Steam pre-creates a virtual POOL whose inputN order doesn't track physical connection order → proximity heuristic claims the WRONG virtual, e.g. the built-in's → built-in drives a player; identity decoupled from input), ran a 4x (draft→adversarial-challenge→revise→finalize) planning workflow (43 flaws + 42 edge cases addressed) for a raw-device rewrite. Plan saved: docs/RAW-CONTROLLER-BIND-PLAN.md.

Implemented the CORE, gated behind CONTROLLER_MONITOR_RAW_BINDING (default OFF — flag-unset = byte-identical legacy behavior, verified):
- controller_monitor.sh: NEW _has_gamepad_buttons (inclusive B:KEY bitmap test, fail-open), NEW _list_raw_external_pads (own /proc parse; js-gate + gamepad-cap + vendor!=28de + shared-parent dedup + (inputN,eventN) sort + dual-transport warn); list_eligible_controllers docked source flag-gated. Built-in is structurally unselectable (no raw js gamepad node).
- instance_lifecycle.sh: NEW _vendor_of_js_node; under raw mode bind js-ONLY (skip eventN — EVIOCGRAB is on the evdev, not the legacy jsN); per-slot SDL_GAMECONTROLLER_ALLOW_STEAM_VIRTUAL_GAMEPAD (0 for a real raw pad, 1 for virtual/handheld). SDL_JOYSTICK_DISABLE_UDEV + udev/pipe hardening preserved.
- orchestrator.sh: comment-only (raw-binding notes + honest disclosure of the pre-existing CONTROLLER_REMOVE zombie-slot defect).
DEFERRED (separate commits): producer-loop hardening (snapshot-skew, udev coalescing, ALREADY_ACQUIRED handoff), dead-code purge, tests rewrite. Deployed to Deck w/ flag ON for user test (launcher test-toggle). NOT yet validated on hardware — user to test.

---

## Raw-device controller bind — VALIDATED 4 players, promoted to DEFAULT (23:03)

Deployed the raw path to the Deck (flag ON) and tested live with the maintainer driving:
- P1 (1 DS4): input alive through the raw js WITH Steam Input active (the critical unknown — RESOLVED yes), shows "Wireless Controller" (real DS4 evdev name, honest identity).
- Incremental add P2→P3→P4: each DS4 bound to its OWN distinct raw js (js2/js5/js7/js9; 3x 054c:09cc + 1x 054c:05c4), js-only, ALLOW=0, ZERO 28de virtuals bound. Built-in AND Steam Controller dead in all 4 instances (structurally excluded — no raw js gamepad node). Each pad controls only its own player, no cross-talk. Maintainer confirmed on-screen at every step.

The exact bug that started the night (incremental P2 gets the built-in) is GONE. Flip criterion met (Controlify reads raw js w/ Steam Input ON + user-confirmed in-game on 4 pads). Promoted CONTROLLER_MONITOR_RAW_BINDING default 0→1 in both read sites; legacy virtual mapper kept as the =0 escape hatch. README status: per-player controller assignment now ✅. Deferred (separate commits): producer-loop hotplug hardening, redundant-masking removal, dead-code purge, test rewrite.

---

## Handheld regression fix — porous sandbox + no duplicate spawn (23:21)

Undocked test exposed two handheld bugs: (1) built-in disabled — my SDL hardening
(DISABLE_UDEV + udev tmpfs + steam.pipe mask) was applied to EVERY instance, but handheld
slot 1 binds NO js (built-in reaches the game as a Steam 28de:11ff virtual, discovered via
udev/pipe) → strict hardening hid it; (2) TWO windows — handheld_flow started the hotplug
monitor, which detected the built-in's own virtual and spawned a phantom slot 2.
Maintainer's framing: docked = strict isolation, handheld = porous.

Fix:
- instance_lifecycle.sh _build_bwrap_command: STRICT (udev tmpfs + steam.pipe mask +
  SDL_JOYSTICK_DISABLE_UDEV=1) now gated on `_strict` = (js_node non-empty). Docked binds a
  real pad's js → strict (validated 4-DS4 behavior preserved). Handheld binds nothing →
  porous → built-in discoverable.
- orchestrator.sh handheld_flow: do NOT start_controller_monitor in handheld (one screen ⇒
  one fixed player; the monitor was spawning the duplicate). Dock-transition still handled by
  watch_display_mode. _CONTROLLER_MONITOR_PID left empty.
Deployed to Deck; NOT yet re-validated undocked (Deck wifi flapped on undock).

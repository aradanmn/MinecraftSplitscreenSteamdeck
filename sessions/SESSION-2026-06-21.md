# Session 2026-06-21 — Re-sync, audit, verify + wire in dex.sh

## RAW TRANSCRIPT

### Context
Continued from 2026-06-20 session (context ran out). Working on Steam Deck external display reliability via dock.

### Issues addressed

**1. Stale WAYLAND_DISPLAY crash loop (both screens black, 2026-06-20 ~04:48)**
- Deck auto-suspended while docked for ~5.5h
- On resume, stale `WAYLAND_DISPLAY=wayland-1` left in systemd --user env (leaked from nested-KWin work)
- gamescope forced into nested backend → "Failed to connect to wayland socket: wayland-1" → crash loop
- 149 failures, both screens black, unrecoverable without manual intervention
- Fix: `systemctl --user unset-environment WAYLAND_DISPLAY` + sddm restart
- Reported to Valve: ValveSoftware/SteamOS#2467 (comment: start-gamescope-session clears DISPLAY/XAUTHORITY but not WAYLAND_DISPLAY)
- Our code was NOT the cause (resume watcher showed no entries, never fired)

**2. Resume watcher never fired**
- Old approach: persistent service tailing `journalctl -f -k` for "PM: suspend exit"
- Problem: journalctl -f process gets frozen during suspend, misses resume event
- Discovery: SteamOS 3.8.10 DOES use systemd-suspend.service (corrects old assumption that SteamOS bypasses systemd-sleep)
- Ships own hooks in /usr/lib/systemd/system-sleep/

**3. Steam not rendering to external on dock connect (2026-06-21)**
- gamescope correctly detected DP-1 (Sceptre O34, 3440×1440)
- But Steam wasn't rendering to it despite valid EDID
- Root: old hotplug script only restarted on missing EDID; Steam-not-rendering also needs restart
- User feedback: "to me it's all the same issue — Steam doesn't behave well with external displays"

### Changes made

**New: system/deck-display-fixes/ in repo**

`dock-display-hotplug.sh` — now restarts gamescope on ANY DP connect event (not just missing EDID). 60s stamp prevents rapid-fire. Env-sanitize before every restart.

`deck-display-resume.sh` — /etc/systemd/system-sleep/ hook, fires post-resume, starts oneshot worker --no-block

`resume-display-restore.sh` — oneshot worker: sleep 8 → sanitize env → restart gamescope if docked. Logs to /tmp/resume-display-restore.log. RESUME_RESTORE_DRYRUN=1 for testing.

`resume-display-restore.service` — Type=oneshot, on-demand (replaces Restart=always journalctl-tail)

`install.sh` — deploys all files to Deck

### Deployed to Deck
- All files installed via git pull + install
- Old resume-display-restore.service (persistent tail) disabled
- Dry-run confirmed: WAYLAND_DISPLAY cleared, external-connected path taken

### Key finding
SteamOS 3.8.10 build 20260617.1 uses systemd-sleep (not /sys/power/state direct write). The /etc/systemd/system-sleep/ hook path is the robust resume trigger.

### Pending
- Live end-to-end test of system-sleep hook (needs real suspend/resume)
- Sceptre O34 OSD: disable "Auto Power Off" to prevent HPD drop during suspend

---

## RAW TRANSCRIPT PUSH 4 — Test design review + _kill_slot_process

User: "the test script is just an observer then we'll never get past T1.2"
→ Clarified: _wait_for_minecraft_ready IS an active check (polls latest.log directly),
  but the teardown trigger was the gap. T1.3 and T7.3 were injecting SLOT_DIED
  directly, bypassing the watchdog entirely.

User: "there needs to be some sort of trigger in the test code to simulate one of
the events needed to test T1.3"
→ Confirmed: docked_flow starts the watchdog (orchestrator.sh:357-359); watchdog
  polls bwrap_pid and java_pid every 2s; when either is gone it writes SLOT_DIED
  to the FIFO. So the watchdog IS live during tests — we just weren't using it.
→ Added `_kill_slot_process()` helper: reads bwrap_pid + java_pid from state file,
  sends SIGTERM to the bwrap process group (negative PID), falls back to java_pid
  if bwrap already exited (SingleApp forwarding case). Does NOT touch state file —
  lets the watchdog detect and drive the teardown.
→ T1.3: `_inject "SLOT_DIED $slot"` → `_kill_slot_process "$slot"` + updated label
→ T7.3: `_inject "SLOT_DIED 2"` → `_kill_slot_process 2` + updated label
→ Cleanup-only SLOT_DIED injects (T2/T3/T4/T5/T6 cleanup, _clean_instances) unchanged
  — direct inject is more reliable for cleanup where watchdog state may be uncertain.
→ Full production path now exercised for crash assertions:
    kill pgid → watchdog detects PID gone → SLOT_DIED → orchestrator teardown → state inactive

END OF RAW TRANSCRIPT PUSH 4

---

## RAW TRANSCRIPT PUSH 5 — Display hotplug EDID stability fix

**Issue:** External Sceptre O34 showing "no signal" + black screens despite gamescope detecting DP-1.

**Root cause identified from logs:**
- Hotplug script was sleeping a flat 8s then restarting gamescope
- At restart time, DP-1 was still mid-MST-enumeration (disconnected in gamescope log)
- Gamescope fell back to eDP-1, then DP-1 appeared 15s later
- Gamescope tried to switch but link training failed silently → monitor "no signal"
- Also: udev rule `/etc/udev/rules.d/99-deck-dock-display.rules` was missing — deployed manually via SSH

**Fix:** `dock-display-hotplug.sh` now polls for EDID stability (up to 45s, same as predock-probe.sh) before restarting gamescope. On disconnect events (no DP connector connected), exits immediately. Falls back to restart anyway on timeout for the broken-probe case.

**Also filed:** Created standalone private GitHub repo `aradanmn/steamdeck-dock-display-fix` with all scripts + CHAT-LOG.md covering the full investigation.

**Deployed:** `d915dfb` — both MinecraftSplitscreenSteamdeck and steamdeck-dock-display-fix repos updated.

END OF RAW TRANSCRIPT PUSH 5

---

## RAW TRANSCRIPT PUSH 6 — Context handoff session (post-compaction)

This session picked up from context compaction. Prior work summarised:
- dex.sh verified working on live gamescope :0 display (5 bugs fixed: pointer truncation,
  wrong valuemask 1<<3, wrong XSetWindowAttributes struct layout, Python docstring syntax
  error, dispatch passing args to no-arg actions)
- dex.sh wired in as single X11 layer: sourced last in minecraftSplitscreen.sh module loop
- window_manager.sh _apply_override_redirect_cycle replaced with dex_set_override_redirect
  + dex_move_resize_force + dex_getgeometry
- instance_lifecycle.sh _poll_for_window rewritten to dex-only (dex_search --name/--pid,
  dex_set_name); all inline ctypes and functional xdotool removed from live modules
- gamescope_windowing.sh confirmed dead code (not sourced anywhere); marked DEAD/UNUSED
- DISPLAY_MODE_CHANGE handheld removed from tests 1-4,6,7 (caused docked_flow to return
  1 → event loop exit → no FIFO reader → all injects hung 200s)
- Session-env guard functions _snapshot_session_env/_restore_session_env added
- Per-run timestamped logs (YYYYMMDD-HHMMSS) with -latest symlinks

Current session work:
→ Explained Phase B test sequence (Tests 1-7) to user after context compaction
→ Clarified start mode: orchestrator always starts in docked_flow regardless of physical
  dock state; test script does not need DISPLAY_MODE_CHANGE for tests 1-4,6,7
→ Clarified teardown mechanism: test injects FIFO messages → docked_flow event loop
  processes them → calls teardown_instance → kills bwrap pgid → updates state file;
  test observes state file + kill -0 PID checks
→ Identified gap: T1.3 and T7.3 were injecting SLOT_DIED directly, bypassing watchdog
→ Added _kill_slot_process() helper; updated T1.3 and T7.3 to use it
  (see PUSH 4 for details)
→ Deck is on c2d7a42, clean, ready for Phase B test run

STATE OF REPO AT HANDOFF (commit c2d7a42 on feat/gamescope-windowing):
- All 7 Phase B tests defined in tests/test_phase_b_lifecycle.sh
- dex.sh is the single X11 layer (modules/dex.sh)
- No functional xdotool in any live module
- Watchdog path tested for crash scenarios (T1.3, T7.3) via _kill_slot_process
- Cleanup-only teardowns still use direct SLOT_DIED inject

PREDICTED NEXT FAILURE (not yet hit):
- SingleApplication forwarding: slots 2-4 PolyMC bwrap exits immediately after
  forwarding to slot 1 primary; bwrap_pid in state goes dead; watchdog may fire
  spurious SLOT_DIED for live forwarded slots. state.json's bwrap_pid field needs
  to be the JAVA pid not bwrap for slots 2-4 (or watchdog needs to check EITHER
  bwrap OR java is alive, not just bwrap).
- Watchdog already checks EITHER (watchdog.sh:64-74): if bwrap_pid dead fires,
  unless java_pid is also checked. Review watchdog logic before concluding.

WHAT HAS NEVER BEEN RUN CLEANLY:
- Any multi-slot test (Tests 2-7) with real Minecraft
- Test 1 got to T1.2 (Minecraft ready after 25s) in a prior run; T1.3 was blocked
  by xdotool hang (now fixed via dex)

END OF RAW TRANSCRIPT PUSH 6

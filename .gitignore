__pycache__/
*.pyc
token.enc
*.log
*.tmp

# private security holding doc — must never be committed (public repo)
ISSUES.md

# Deck-side probe/benchmark scratch (issue #122 — one-shot wipeable)
.workdir/
